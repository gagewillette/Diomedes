{
  "name": "diomedes",
  "version": "2.0.10",
  "private": true,
  "scripts": {
    "dev": "bash scripts/dev.sh",
    "dev:down": "COMPOSE_PROFILES=ollama docker compose -f docker-compose.dev.yml down"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "wait-on": "^8.0.1"
  }
}
